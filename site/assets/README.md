# Landing-page assets (`site/assets`)

Static images/CSS for the public landing page (`site/index.php`).

Missing referenced images to add here: `card-community.jpg`, `card-events.jpg`,
`card-performance.jpg` (referenced by `site/index.php` — supply the real
marketing images).
